# 🚀 폴더정리 자동화 프로젝트 개발 노트

## 1. 프로젝트 개요
바탕화면, 다운로드 등 어지럽혀진 폴더 내 파일들을 사용자가 정한 규칙(연도, 주제)에 따라 안전하고 일관성 있게 자동 분류 및 이동해 주는 스마트 애플리케이션 데스크톱 도구입니다. 복잡한 의존성 없이 로컬 환경에서 단독 실행(`standalone EXE`)되도록 개발되었습니다.

## 2. 개발 스택 및 기술 구조
- **언어:** Python 3.14
- **주요 라이브러리:**
  - `customtkinter`: 현대적이고 유려한 다크/라이트 모드와 테마 전환을 지원하는 최신 프리미엄 GUI 프레임워크 
  - `pyinstaller`: Python 스크립트 및 에셋들을 윈도우 단일 실행 파일(`.exe`)로 패키징
  - `rembg`, `Pillow`: AI 기반으로 데스크톱 앱 아이콘의 배경(여백)을 제거하고 `.ico` 해상도 최적화 적용
  - `click`, `rich`: 개발 및 디버깅을 위한 미려한 터미널 출력 및 파서 (핵심 CLI 기능 유지)
  - `yaml`: 사용자 커스텀 설정(분류 규칙)을 유연하게 읽고 쓰기 위한 데이터 직렬화

## 3. 핵심 시스템 아키텍처
기본적인 설계 철학은 "설정 주도형(Config-Driven) 분류 시스템"입니다. 
- `config_loader.py`: 초기 실행 시 `~/.organizer/config.yaml` 환경 설정이 없으면 자동으로 기본 분류 체계를 가진 설정 파일을 생성하여 주입합니다.
- `classifier.py`: 파일의 확장자나 파일명에 포함된 다중 키워드를 검사하여 사전에 설정된 테마(문서, 이미지, 교육자료 등)로 스마트하게 분류합니다. 
- `mover.py`: 파일 이동 작업을 담당하며, 중복 파일 발생 시 영리하게 `(1)`, `(2)`와 같은 접미사를 자동으로 붙여 데이터 유실을 100% 방지합니다 (비파괴적 정리 방식).
- `history.py`: `undo`(실행 취소) 기능을 지원하여 바로 직전까지 이동된 수십~수백 개의 폴더 경로를 기억하고 원상복구 시키는 트랜잭션 매니저 역할을 수행합니다.
- `gui.py` & `settings_gui.py`: 사용자가 터미널 환경에 익숙하지 않아도 위의 모든 로직을 컨트롤하고, 규칙을 자유롭게 튜닝하며, 시각적인 진행 상태를 볼 수 있도록 구현한 메인 프론트엔드입니다.

## 4. 특이사항 및 해결했던 문제점
1. **OneDrive (클라우드 파일) 오류 처리:** 로컬 디스크에 완전히 다운로드되어 있지 않고 구름 아이콘(디맨드 전용) 상태인 파일은 이동 중 접근 오류(`WinError 362`)가 발생했습니다. 이를 방어하기 위해 예외 처리를 추가하여 비정상 파일은 스킵하고 로그로만 알리도록 수정했습니다.
2. **PyInstaller 경로 탐색 문제:** `__file__` 기반의 상대 경로 로직이 Frozen 바이너리(빌드된 .exe) 환경에서 작동하지 않는 것을 해결하기 위해 `getattr(sys, 'frozen', False)` 유틸을 활용, 실행 위치와 설정 파일(config.yaml) 위치를 분리 대응(`~/.organizer` 활용)하였습니다.
3. **GUI 테마 및 아이콘 이슈:** 기본 Tkinter가 윈도우 버전에 따라 다르게 보이고, 빌드된 파일의 아이콘 배경이 하얗게 남는 문제가 있었습니다. UI 프레임워크를 `customtkinter`로 통째로 전환하고 `rembg`를 통해 시각 디자이너 수준의 투명도를 확보한 에셋을 재결합했습니다.

## 5. 배포 체계 및 아카이빙 내역
개발 과정에서 사용되었지만 일반 사용자에게는 노출될 필요가 없는 임시/빌드 폴더(`build`, `fixtures`, `.spec`, 파이썬 단축 아이콘 스크립트 등)는 프로젝트 내부 **`dev_archive/`** 디렉터리로 모두 깔끔하게 격리(이동)했습니다. 

최종 사용자는 프로젝트 최상위의 **`폴더정리_자동화_단일실행파일.zip`** 만 전달받아 즉시 사용할 수 있습니다.
